export 'enum_ex.dart';

enum UserRole {
  admin,
  serviceTech,
  customer,
}

enum CTContactType {
  primary,
  billing,
  schedule,
}

enum CustomerCategory {
  jobs,
  siteProfiles,
  contacts,
  pictures,
  notes,
  subAccounts
}